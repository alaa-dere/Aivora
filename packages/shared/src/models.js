/**
 * Shared data shape defaults used by both web and mobile.
 * Keep these values framework-agnostic.
 */
export const EMPTY_SESSION = {
  token: null,
  role: null,
  user: null,
};

export const EMPTY_PAGINATION = {
  page: 1,
  pageSize: 10,
  total: 0,
};
